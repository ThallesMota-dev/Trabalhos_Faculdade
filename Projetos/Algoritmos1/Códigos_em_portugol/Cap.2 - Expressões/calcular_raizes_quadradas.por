Algoritmo Raízes

var delta, x1, x2, a, b, c: real;
início
    a ← 2;
    b ← 4;
    c ← -3;
    delta ← b pot 2 - 4 * a * c;
    x1 ← (-b + (delta raiz 2)) / 2 * a;
    x2 ← (-b - (delta raiz 2)) / 2 * a;
fim